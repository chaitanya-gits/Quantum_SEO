apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: quantum-seo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: pgvector/pgvector:pg17
          env:
            - name: POSTGRES_DB
              value: quantum_seo
            - name: POSTGRES_USER
              value: quantum
            - name: POSTGRES_PASSWORD
              value: quantum
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: quantum-seo
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
